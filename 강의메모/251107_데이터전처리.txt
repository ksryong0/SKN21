범주형
	Label Encoding
	One-Hot Encoding
수치형 : 같은 기준의 값으로 바꿔줌. 값들의 단위수가 각각 다르기 때문에.
	Standard Scaling:평균0 표준편차1로 맞춰줌.
	MinMax Scaling:최소0, 최대1로 바꿔버림.

Feature Scaling
전처리
	trainset : 모델한테 학습시킬 데이터. 전처리를 이 데이터셋을 가지고 해야함.
	test(val) : 평가. 학습은 안시킬 데이터.

1.valid 제일 좋은거
2.valid 제일 좋은게 여러개일 때 : train과 valid 차이가 적은 걸 쓰면 됨

정밀도는 예측기준. 맞/ 예측 P
리콜은 실제 정답기준. 맞/전체 P
리콜과 정밀도가 비슷한 모델이 좋음.

재현율
FN를 줄이려 함.
암환자 판정 모델.
환자를 정상이라고 하거나,	-> 환자를 정상이라고 하는게 더 문제.     -> 
정상을 환자라고 하거나.

정밀도
FP를 낮춰야됨
스팸메일을 정상이라고 하거나,	-> 
정상을 스팸메일이라고 할떄		정상메일을 스팸이라고 해서 지워버리면 큰 문제가 됨.

재현율을 올리면 정밀도가 떨어지고, 정밀도를 올리면 재현율이 떨어짐.
threshold를 올리면 재현율이 낮아지고 정밀도가 올라감.
threshold를 내리면 재현율이 올라가고 정밀도가 떨어짐. -> 양성이라고 판단하는게 많아짐.

그래프의 면적이 클수록 강건한 모델. Average Precision Score 로 면적을 구함.

FPR과 TPR은 비례. 임계값을 올리면 FPR이 낮아짐
fpr 과 리콜은 비례. 임계값을 낮추면 FPR과 리콜은 올라감.

FPR-TPR 그래프
그래프 면적이 넓은게 좋은 모델임. A>B>C

FPR은 낮을수록 좋음.
TPR은 높을수록 좋음.

FPR-TPR 그래프는 처음부터 높은게 좋음. 

30 30+1=31
30<31 30
30 28 +1 = 29
30 29 +1 = 30
load_xxx : 
fetch_xxx :
make_xxx :  