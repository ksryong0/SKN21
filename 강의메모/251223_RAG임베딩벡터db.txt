청킹 이유:
임베딩 모델, LLM의 입력 컨텍스트 길이 제한.
정확도 향상.

CharacterTextSplitter:구분자 1개. 디폴트 엔터 두개.
Recursive...:구분자 여러개.

split_documents 함수를 쓰면 문서를 스플릿시켜준다. 인자로 리스트를 넣어야됨.
---------------------- 복습 -------------------------
MarkdownHeaderTextSplitter:마크다운 기준으로 스플릿.

08_RAG 임베딩_벡터DB

질문의 임베딩벡터와 문서의 임베딩벡터의 유사도를 계산.

임베딩
문서, 임베딩 모델(질문과 문장에 대한 유사도를 검증)

embed_documents:스플릿한 문서를 임베딩
임베드쿼리:질문을 임베딩

https://platform.openai.com/
이동

질문 문서
같은 방향을 볼 때. 크기는 다를수있음. 같은 방향이라는건 같은 의미로 볼 수 있음.
다른 방향을 볼 때 다른 의미. 관계가 적다.
코사인 유사도.

cmd 창에서 
ollama pull bge-m3
입력

정형데이터는 일치하는거로 찾으면 됨.
비정형데이터는 딱 일치하지 않기 때문에 유사도로 검색해야함

KNN은 시간이 오래걸리기 때문에 변형알고리즘인 ANN을 이용.

upsort?
벡터스토어를 만든 이유. RAG하려고.

retriever
context찾는거까지 하면 리트리버가 나머지는 알아서 해주는듯.

어떤 질문과 비슷한 문서를 10개 찾으면, 그 문서 10개끼리도 비슷.

유사도만 보지 말고, 문서간의 차이도 좀 보자

람다*sim (문서간의 관련성)
- (1-람다)* maxsim ( 문서간의 다양성)
d는 점수를 내려고 하는 문서
d'는 지금까지 유사도 점수가 가장 높은 애

큐드란트용 랭체인 라이브러리가 있음

docker 설치

windows 기준
1. wsl 설치
	- powershell 설정
	- 'wsl --install Ubuntu'
->안되서 'wsl.exe --install Ubuntu' 이렇게 설치함

2. Docker Desktop 설치
구글에서 docker 검색.
sign in 해서 가입.
Docker Desktop 선택
윈도우는 amd64 다운로드
실행

도커 : 어플리케이션이 실행할 수 있는 환경을 이미지로 만들어서(패키징)
웹서버 파이썬 앱 os 등등을 한번에 묶어서 설치


