선형적으로 발전하다가 어느순간 확 좋아짐.
LLM은 트랜스포머 아키텍처 기반. 레이어를 깊게 쌓음. 방대한 양의 텍스트 학습 가능.
파란가지 디코더 계열. GPT가 대표.

라지
모델의 크기
훈련 데이터셋

기준이 바뀌고 있긴 하지만
7B보다 작으면 sLLM
7B이상이면 LLM

말을 생성하는데 특화.

파운데이션 모델
LLM을 파운데이션 모델이라고도 함.(범용적 특성)

멀티모달-다양한 입출력(이미지, 텍스트, 음성)

오픈소스 LLM 
클로즈드 소스 LLM

랭체인 프레임워크
오픈소스프레임워크.
gpt가 됐건 제미나이가 있건 같은 방식으로 사용할 수 있음.

에이전트:이거해줘 하면 해주는애. 사용자가 원하는걸 해 줄 때까지 일을 하는 애. 비서같은 느낌. 

모델 I/O: LLM한테 질문(프롬프트). 답변 받는거(Output Parser).

LLM문제
knowledge cutoff :특정시점 이전의 학습 정보는 없음.

벡터디비:데이터를 저장하는데, 검색하기위한 키워드(인덱스)를 벡터값으로 저장.
RDB:

체인
연결되는작업들을 파이프라인으로 묶어준거.
장점:재사용성 높아짐

에이전트
랭체인에서는 랭그래프를 이용.
LLM에 툴을 붙여줌. 툴은 함수.
LLM은 어떤툴을 쓸건지판단. 이 결과가 질문에 답하기 충분한가?
안충분하면 다른 툴을 씀.

max_tokens:대답이 max_tokens길이를 넘어가면 답변이 중간에 끊길수도 있음. 그럼 프롬프트로 해야됨.

모델 IO 구성 요소

언어 모델:	

프롬프트 엔지니어링 필요한 이유?
내부적으로 프롬프트 최적화가 되서 LLM한테 보내주기 때문에.

모델->파인튜닝->

프롬프트 작성시 명확성, 구체성 이 가장 중요

