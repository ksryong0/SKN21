데이터베이스 : 데이터 관리 프로그램
엔티티 - 속성
데이터베이스는 영구적으로 저장되어야 함

DBMS가 하나만 있어야되는 이유? 한군데서 업데이트한 것을, 다른 데서는 모름
DBMS는 서버 프로그램으로 실행. 다른 컴퓨터에서 네트워크 연결하여 사용.

SQL?
SELECT는 따로 DQL(Query)라고도 함

1. 데이터베이스
2. 데이터->엔티티(속성)->테이블
테이블은 행과 열로 구성
DDL-CREATE

플러스버튼
connection name : playdata계정
username:playdata

varchar에서 길이를 생략하면 1로 세팅됨.

sql의 에러는 에러코드로 검색할 수 있음!!
