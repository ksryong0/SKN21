딥러닝 모델

gpu 테스트
구글 드라이브
런타임->런타임 유형 변경->T4 GPU

t.to(cpu 또는 cuda )

같은 메모리상에 있는 텐서끼리만 연산이 가능.
torch.cuda.is_available()

reshape이나 view를 하면 참고하는 객체를 바꿨을 때, 원본도 바뀐다.
a = torch.rand(12)
a2 = a.reshape(3,4)
a2[0, 1] = 15.1
이렇게하면 a도 바뀜

a는 안바뀌고 a2만 바뀌게 하려면 clone을 사용
a2 = a.reshape(3, 4).clone()
a2[0,0] = 100.1
print(a)

unsqueeze 축 늘리는거

squeeze 더미축 제거

어제 배운 거
------------------
텐서합치기
합치는 기준 축을 제외한 나머지 축 사이즈는 같아야 한다.
합칠 때, dim으로 쓰는게 좋음.
dim 디폴트는 0

딥러닝 기본개념
x ->LR->LR->LR->LR->y
LR(Linear Regression)

reshape은 값의 순서는 안바뀜
transpose는 구조 자체가 바뀜. 따라서 값도 바뀜.

자연어
(100,	10,		3)
문장	단어수	단어의 피쳐수(개별 단어의 글자수)

이미지	3차원
영상		5차원
자연어	3차원

축과 축의 의미가 중요.

행렬곱:앞에 행수와 뒤에 열수가 같아야됨.
옵티마이저 = loss - lr*도함수 계산해주는애
--------------
오토그래드
마지막 시간에 한거 중요